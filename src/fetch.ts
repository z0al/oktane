// Packages
import {} from 'redux-saga/effects';

// Ours
import { Request } from './utils/request';
import { ResolverFn } from './utils/resolver';

function* fetch(_req: Request, _fn: ResolverFn) {}

export default fetch;
